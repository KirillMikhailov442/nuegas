'use client'

import {extendTheme} from '@chakra-ui/react'

const chakraTheme = extendTheme({
    colors: {
        primary: {
            500: '#546fff'
        }
    }
})

export default chakraTheme